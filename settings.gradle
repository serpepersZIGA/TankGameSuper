pluginManagement {
    plugins {
        id "org.jetbrains.kotlin.plugin.lombok" version '2.4.10'
    }
}
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'TankGameSuper'

include 'desktop', 'core'
